# Build / packaging artifacts
*.zip
release/      # output of tools/make_release.py

# Simulator output & logs
*.raw
*.log
b3v33check.log
*.out

# Python build artefacts
__pycache__/
*.pyc
*.pyo

# OS / editor cruft
.DS_Store
Thumbs.db
*~
.*.swp

# Local, uncommitted working files (contain third-party process data —
# must never be committed to a public repo). Added for the ONC25 grounding
# extraction; the committed docs carry derived/scaled values only.
LOCAL_*
